# ml-service
РЕКОМЕНДУЕТСЯ ЗАПУСКАТЬ ЧЕРЕЗ ОСНОВНУЮ ИНСТРУКЦИЮ!!!!

Embed, synthesize, Yandex GPT. Запуск в составе стека:

```bash
cd ../server-service
cp .env.example .env
chmod +x deploy.sh && ./deploy.sh
```

http://localhost:8001/health
